# Pin this repo to the public npm registry so dependencies resolve to
# registry.npmjs.org regardless of a developer's global ~/.npmrc (for example
# a corporate mirror). Without this, an install rewrites the resolved URLs in
# package-lock.json to that mirror, which leaks internal infrastructure into a
# public repository and breaks installs for everyone else, CI included.
registry=https://registry.npmjs.org/
